/**
 * =============================================================================
 * API CONFIG — Future backend configuration
 * =============================================================================
 * When connecting a real backend, configure the base URL and headers here.
 * All service files will import from this config.
 * =============================================================================
 */

export const API_BASE_URL = "";
export const API_HEADERS = {
  "Content-Type": "application/json",
};
