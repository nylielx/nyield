/**
 * =============================================================================
 * MARKETPLACE TYPES — Type definitions for the marketplace feature
 * =============================================================================
 *
 * Re-exports the MarketplaceListing type from the data layer.
 * Keeps imports clean within the marketplace page module.
 * =============================================================================
 */

export type { MarketplaceListing } from "@/data/marketplaceExamples";
